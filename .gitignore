# Build output
/bin/
*.exe

# Redundant: the canonical source now lives in the tree
conclave.tar.gz

# Editor / OS
.DS_Store
*.swp

# Local tooling index (not part of the project)
.codegraph/

# pc's own runtime artifacts: `pc init`'s scenario/database and
# scripts/live-run.sh's per-run binary, database, logs, and worktrees.
# Uncommitted deliberately — a compiled binary, a SQLite database, and git
# worktrees have no business in a `git add -A` from the project root.
.pc/
.pc.yaml

# SDD scratch (ledger, briefs, review packages)
.superpowers/
